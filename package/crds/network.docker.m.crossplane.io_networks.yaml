---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: networks.network.docker.m.crossplane.io
spec:
  group: network.docker.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - docker
    kind: Network
    listKind: NetworkList
    plural: networks
    singular: network
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .status.atProvider.driver
      name: DRIVER
      priority: 1
      type: string
    - jsonPath: .status.atProvider.scope
      name: SCOPE
      priority: 1
      type: string
    - jsonPath: .status.atProvider.internal
      name: INTERNAL
      priority: 1
      type: boolean
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A Network is a managed resource that represents a Docker network.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A NetworkSpec defines the desired state of a Network.
            properties:
              forProvider:
                description: ForProvider contains the provider-specific configuration.
                properties:
                  attachable:
                    description: Attachable enables manual container attachment to
                      the network.
                    type: boolean
                  driver:
                    default: bridge
                    description: |-
                      Driver specifies the network driver to use.
                      Common drivers: bridge, overlay, host, none, macvlan
                    type: string
                  enableIPv6:
                    description: EnableIPv6 enables IPv6 networking.
                    type: boolean
                  ingress:
                    description: Ingress designates the network as providing the routing-mesh.
                    type: boolean
                  internal:
                    description: Internal restricts external access to the network.
                    type: boolean
                  ipam:
                    description: IPAM contains IP Address Management configuration.
                    properties:
                      config:
                        description: Config contains IPAM configuration.
                        items:
                          description: IPAMConfigEntry represents a single IPAM configuration
                            entry.
                          properties:
                            auxAddresses:
                              additionalProperties:
                                type: string
                              description: AuxAddresses are auxiliary IPv4 or IPv6
                                addresses used by the network driver.
                              type: object
                            gateway:
                              description: Gateway is the gateway for the master subnet.
                              type: string
                            ipRange:
                              description: IPRange limits allocation to a subset of
                                the subnet.
                              type: string
                            subnet:
                              description: Subnet is the subnet in CIDR format that
                                represents a network segment.
                              type: string
                          type: object
                        type: array
                      driver:
                        default: default
                        description: Driver specifies the IPAM driver to use.
                        type: string
                      options:
                        additionalProperties:
                          type: string
                        description: Options is a map of driver-specific IPAM options.
                        type: object
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels is a map of labels to apply to the network.
                    type: object
                  name:
                    description: Name is the network name. If not specified, the resource
                      name will be used.
                    type: string
                  options:
                    additionalProperties:
                      type: string
                    description: Options is a map of driver-specific options.
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A NetworkStatus represents the observed state of a Network.
            properties:
              atProvider:
                description: AtProvider contains the observed state of the Network.
                properties:
                  attachable:
                    description: Attachable indicates if the network allows manual
                      attachment.
                    type: boolean
                  containers:
                    additionalProperties:
                      description: NetworkContainer represents a container attached
                        to the network.
                      properties:
                        endpointID:
                          description: EndpointID is the endpoint identifier.
                          type: string
                        ipv4Address:
                          description: IPv4Address is the IPv4 address of the container
                            on this network.
                          type: string
                        ipv6Address:
                          description: IPv6Address is the IPv6 address of the container
                            on this network.
                          type: string
                        macAddress:
                          description: MacAddress is the MAC address of the container
                            on this network.
                          type: string
                        name:
                          description: Name is the container name.
                          type: string
                      type: object
                    description: Containers is a map of containers attached to the
                      network.
                    type: object
                  createdAt:
                    description: CreatedAt is the timestamp when the network was created.
                    format: date-time
                    type: string
                  driver:
                    description: Driver is the network driver being used.
                    type: string
                  enableIPv6:
                    description: EnableIPv6 indicates if IPv6 is enabled.
                    type: boolean
                  id:
                    description: ID is the unique identifier of the network.
                    type: string
                  ingress:
                    description: Ingress indicates if this is an ingress network.
                    type: boolean
                  internal:
                    description: Internal indicates if the network is internal-only.
                    type: boolean
                  ipam:
                    description: IPAM contains the current IPAM configuration.
                    properties:
                      config:
                        description: Config contains IPAM configuration.
                        items:
                          description: IPAMConfigEntry represents a single IPAM configuration
                            entry.
                          properties:
                            auxAddresses:
                              additionalProperties:
                                type: string
                              description: AuxAddresses are auxiliary IPv4 or IPv6
                                addresses used by the network driver.
                              type: object
                            gateway:
                              description: Gateway is the gateway for the master subnet.
                              type: string
                            ipRange:
                              description: IPRange limits allocation to a subset of
                                the subnet.
                              type: string
                            subnet:
                              description: Subnet is the subnet in CIDR format that
                                represents a network segment.
                              type: string
                          type: object
                        type: array
                      driver:
                        default: default
                        description: Driver specifies the IPAM driver to use.
                        type: string
                      options:
                        additionalProperties:
                          type: string
                        description: Options is a map of driver-specific IPAM options.
                        type: object
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels are the labels applied to the network.
                    type: object
                  name:
                    description: Name is the actual network name.
                    type: string
                  options:
                    additionalProperties:
                      type: string
                    description: Options are the driver-specific options for the network.
                    type: object
                  scope:
                    description: Scope indicates whether the network is local or global.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
